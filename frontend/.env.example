# Angular currently reads these values from src/environments/environment*.ts.
# Keep this file as deployment documentation or wire it into your own build pipeline.
NG_APP_API_BASE_URL=http://localhost:4000/api
NG_APP_ASSET_BASE_URL=http://localhost:4000
